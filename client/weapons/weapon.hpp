#pragma once

namespace client {
/// @brief The namespace containing all weapons.
namespace weapon {

enum WeaponSlot {
    COMBAT,
    SPECIAL,
    BOTH
};

enum WeaponType {
    BLANK,
    ZORD
};

} // namespace weapons
} // namespace client
